#include "RPN.hpp"

int main(int argc, char **argv) {
    // whats an inverted polish notation mathematical expression?
    // numbers must be less than 10, single digit parameters, but the result is free
    // output on standard output
    // prints error messages if anything is wrong
    // handle + - * /
    // no need to handle brackets or decimals std::cout << "Error" << std::endl;
    return 0;
}